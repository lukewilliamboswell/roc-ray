generated-docs/

# Built example executables (each Roc app is examples/<name>/main.roc)
examples/*/main
examples/*/main.exe

# Built executables at root
/main
/hello_world
/keyboard
/input_inspector
/kitchen_sink
/read_env
/pong
/sprites
/text_ui
/snake
/breakout
/camera
/top_down
/cave_climb
/generated_assets
/post_process
/projective_texture
/responsive_ui

all_roc_syntax

build
app

# Ignore markdown files, except main README and CONTRIBUTING
*.md
!README.md
!CONTRIBUTING.md
# `zig build lint` reads this one, so losing it breaks the build rather than
# quietly dropping a note.
!docs/action-coverage.md

# Build artifacts (but NOT vendored libraries)
*.a
*.tar.br
*.tar.gz
*.tar.zst
*.ll
*.lib
*.dylib
*.so
*.lib
*.o
*.rh
*.rm

# Exception: keep vendored raylib libraries (at top-level vendor/)
!vendor/raylib/**/*.a
!vendor/raylib/**/*.so
!vendor/raylib/**/*.dylib
!vendor/raylib/**/*.lib

# Exception: keep CRT runtime files for cross-compilation
!platform/targets/arm64glibc/Scrt1.o
!platform/targets/arm64glibc/crti.o
!platform/targets/arm64glibc/crtn.o
!platform/targets/x64glibc/Scrt1.o
!platform/targets/x64glibc/crti.o
!platform/targets/x64glibc/crtn.o

.claude

host/libhost.h

zig-cache/
zig-out/
build
app

.DS_Store
.bundle-stage-*/

# generated files
#host/roc/
host/glue.zig
host/glueForArch/
host/types.json

# ingore things that are WIP
wip-*

.zig-cache/
platform/dynhost

# ignore the libapp that is built when making a dynhost
libapp

# generated by ./ci/all_tests.sh
roc-src/
build-output/

# log files
*.log

# Python cache
__pycache__/

# Windows executables
*.exe
# Legacy root-level executable.
/async_read

# Generated CLI integration executable.
/test/cli_args/main

# Generated model-allocation probe executable.
/test/model_inplace/main

# Output written by the capture examples
captures/
shots/
