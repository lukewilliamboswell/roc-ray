generated-docs/

examples/hello
examples/exit_code_msg
examples/exit_tag

# Built executables at root
hello_world

all_roc_syntax

build
app

# Ignore markdown files, unless explicitly included
*.md

# Ignore Roc Ray Simulation Files
*.rrsim

# Build artifacts (but NOT vendored libraries)
*.a
*.tar.br
*.tar.gz
*.tar.zst
*.ll
*.lib
*.dylib
*.so
*.lib
*.o
*.rh
*.rm

# Exception: keep vendored raylib libraries (at top-level vendor/)
!vendor/raylib/**/*.a
!vendor/raylib/**/*.so
!vendor/raylib/**/*.dylib
!vendor/raylib/**/*.lib

# Exception: keep CRT runtime files for cross-compilation
!platform/targets/arm64glibc/Scrt1.o
!platform/targets/arm64glibc/crti.o
!platform/targets/arm64glibc/crtn.o
!platform/targets/x64glibc/Scrt1.o
!platform/targets/x64glibc/crti.o
!platform/targets/x64glibc/crtn.o

.claude

host/libhost.h

zig-cache/
zig-out/
build
app

.DS_Store

# generated files
#host/roc/
host/glue.zig
host/glueForArch/
host/types.json

# ingore things that are WIP
wip-*

.zig-cache/
platform/dynhost

# ignore the libapp that is built when making a dynhost
libapp

# generated by ./ci/all_tests.sh
roc-src/
build-output/

# WASM development server output
www/

# WASM build outputs (root level only, not platform/web/)
/*.wasm
/app.wasm
/host.js
/index.html
